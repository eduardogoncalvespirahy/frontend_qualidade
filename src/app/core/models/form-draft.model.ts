export interface FormDraft {
  formId: string;
  rascunhoData: string;
}

export interface CreateFormDraft {
  formId: string;
  rascunhoData: string;
}

export interface UpdateFormDraft {
  formId: string;
  rascunhoData: string;
}
